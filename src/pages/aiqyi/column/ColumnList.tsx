import React from 'react';
import { connect } from 'umi';

interface KehuanProps {}

const ColumnList: React.FC<KehuanProps> = props => {
  return <div>list</div>;
};

export default connect()(ColumnList);
